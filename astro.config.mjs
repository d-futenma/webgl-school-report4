import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://d-futenma.github.io',
  base: '/webgl-school-report4',
  server: {
    host: true,
  },
});
